package lol.hub.headlessbot.behaviour.nodes.leaf;

import lol.hub.headlessbot.behaviour.nodes.Node;

public abstract class TaskNode extends LeafNode {
    public TaskNode(Node parent) {
        super(parent);
    }
}
